* Channel #FOO has no topic
* Channel mode for #FOO is +t
* Channel #foo has no topic
* Channel mode for #foo is +t
* Channel #foo has no topic
* Channel mode for #foo is +t
* Channel #foo has no topic
* Channel mode for #foo is +t
* Channel #FOO has no topic
* Channel mode for #FOO is +t
* Channel #FOO has no topic
* Channel mode for #FOO is +t
* Channel #FOO has no topic
* Channel mode for #FOO is +t
* Channel #foo has no topic
* Channel mode for #foo is +t
* Channel #foo has no topic
*** TheMoog (~TheMoog@loopback) has left IRC (Ping time-out)
* Channel mode for #foo is +t
* Channel #FOO has no topic
* Channel mode for #FOO is +t
* Channel #FOO has no topic
* Channel mode for #FOO is +t
* Channel #FOO has no topic
* Channel mode for #FOO is +t
* Channel #FOO has no topic
* Channel mode for #FOO is +t
* Channel #FOO has no topic
* Channel mode for #FOO is +t
* Channel #FOO has no topic
* Channel mode for #FOO is +t
* Channel #foo has no topic
* Channel mode for #foo is +t
* Channel #foo has no topic
* Channel mode for #foo is +t
* Channel #foo has no topic
* Channel mode for #foo is +t
* Channel #foo has no topic
* Channel mode for #foo is +t
* Channel #foo has no topic
* Channel mode for #foo is +t
* Channel #foo has no topic
* Channel mode for #foo is +t
* Channel #FOO has no topic
* Channel mode for #FOO is +t
* Channel #FOO has no topic
* Channel mode for #FOO is +t
* Channel #FOO has no topic
* Channel mode for #FOO is +t
* Channel #FOO has no topic
* Channel mode for #FOO is +t
* Channel #FOO has no topic
* Channel mode for #FOO is +t
* Channel #FOO has no topic
* Channel mode for #FOO is +t
* Channel #FOO has no topic
* Channel mode for #FOO is +t
* Channel #FOO has no topic
* Channel mode for #FOO is +t
* Channel #FOO has no topic
* Channel mode for #FOO is +t
* Channel #foo has no topic
* Channel mode for #foo is +t
* Channel #FOO has no topic
* Channel mode for #FOO is +t
* Channel #foo has no topic
* Channel mode for #foo is +t
* Channel #foo has no topic
* Channel mode for #foo is +t
* Channel #foo has no topic
* Channel mode for #foo is +t

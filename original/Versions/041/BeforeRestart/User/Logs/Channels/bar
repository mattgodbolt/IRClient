* Channel #bar has no topic
* Channel mode for #bar is +t
* Channel #bar has no topic
* Channel mode for #bar is +t
* Channel #bar has no topic
* Channel mode for #bar is +t
* Channel #BAR has no topic
* Channel mode for #BAR is +t
* Channel #BAR has no topic
* Channel mode for #BAR is +t
* Channel #BAR has no topic
* Channel mode for #BAR is +t
* Channel #BAR has no topic
* Channel mode for #BAR is +t
* Channel #BAR has no topic
* Channel mode for #BAR is +t
* Channel #BAR has no topic
* Channel mode for #BAR is +t
* Channel #BAR has no topic
* Channel mode for #BAR is +t
* Channel #BAR has no topic
* Channel mode for #BAR is +t
* Channel #BAR has no topic
* Channel mode for #BAR is +t
